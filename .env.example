# Server
PORT=3000

# Database
# Path to the SQLite database file (default: tickets.db in the project root)
DB_PATH=./tickets.db

# Maximum number of connection attempts before giving up
DB_MAX_RETRIES=5

# Delay in milliseconds between consecutive connection attempts
DB_RETRY_DELAY_MS=1000
